# node.type.mlfruitandnut.yml
# If we don't add this dependency, Drupal will not remove the content
# type when we uninstall our module.
# When a site builder decides that this module is no longer required,
# we don't want to have the content type available anymore.

langcode: en
status: true
dependencies:
  enforced:
    module:
      - mlfruitandnut
name: 'Main Lab Fruit and Nut'
type: mlfruitandnut
description: 'Content type that can be used to provide additional information on <em>mlfruitandnut</em>'
help: ''
new_revision: false
preview_mode: 1
display_submitted: true